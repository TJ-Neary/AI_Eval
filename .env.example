# =============================================================================
# AI_Eval — Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# The .env file is gitignored and should NEVER be committed to version control.
# Only uncomment and set the variables you need for your evaluation setup.
# =============================================================================

# ── API Keys ─────────────────────────────────────────────────────────────────
# Provider API keys for cloud model evaluation.
# Leave commented if only evaluating local models via Ollama.

# Google Gemini API — https://ai.google.dev/
# GOOGLE_API_KEY=your-google-api-key-here

# Anthropic Claude API — https://console.anthropic.com/
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI GPT API — https://platform.openai.com/api-keys
# OPENAI_API_KEY=your-openai-api-key-here

# ── Paths ────────────────────────────────────────────────────────────────────
# State directory for evaluation data, logs, and cached results.
# Default: ~/.ai_eval

# DATA_DIR=~/.ai_eval

# ── Logging ──────────────────────────────────────────────────────────────────
# Logging verbosity. Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO

# LOG_LEVEL=INFO

# Enable debug mode for verbose output and additional diagnostics.
# Default: false

# DEBUG=false
