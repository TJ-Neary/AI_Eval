# AI_Eval Configuration
# Copy this file to .env and fill in your values.
# The .env file is gitignored and should NEVER be committed.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your values

# ── Required Paths ──────────────────────────────────────────────────────────
# Adjust these for your environment. Use absolute paths or ~ for home dir.

# DATA_DIR=~/.ai_eval





# ── API Keys (Optional) ────────────────────────────────────────────────────
# Only add keys you actually need. Leave commented if unused.
# GOOGLE_API_KEY=your_api_key_here